#include <stdio.h>
#include <inttypes.h>

#include "CarControl.h"

int main() {
	setLeftSideForward();
	setRightSideForward();

	setLeftSideSpeed(100);
	setRightSideSpeed(100);

	setLeftSideSpeed(75);
	setRightSideSpeed(75);

	setLeftSideSpeed(50);
	setRightSideSpeed(50);

	setLeftSideSpeed(25);
	setRightSideSpeed(25);

	setLeftSideSpeed(0);
	setRightSideSpeed(0);

	while(1) {

	}
}
