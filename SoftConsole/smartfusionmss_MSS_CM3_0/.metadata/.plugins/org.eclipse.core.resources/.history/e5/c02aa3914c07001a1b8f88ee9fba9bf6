#include "xbee.h"

void initialize_controller_reciever(void) {

}

void controller_data_rx_handler( mss_uart_instance_t * this_uart ) {
	uint8_t rx_buff[20];
	//MSS_UART_get_rx( this_uart, &rx_buff[g_rx_idx], sizeof(rx_buff) );
}
