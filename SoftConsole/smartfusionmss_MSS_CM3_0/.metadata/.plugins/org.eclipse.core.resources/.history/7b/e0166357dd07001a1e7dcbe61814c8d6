#include <stdio.h>
#include <inttypes.h>
#include <string.h>

#include "CarControl.h"
#include "xbee.h"

int main() {
	setLeftSideForward();
	setRightSideForward();

	setLeftSideSpeed(64);
	setRightSideSpeed(64);

	setLeftSideReverse();
	setRightSideReverse();

	setLeftSideSpeed(128);
	setRightSideSpeed(128);

	setLeftSideSpeed(0);
	setRightSideSpeed(0);

	//strcpy(recieved_data, "hello world!");

	while(1) {
		printf("%s\r\n", recieved_data);
	}
}
